PORT=5000
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/sommys_store?retryWrites=true&w=majority
JWT_SECRET=your_jwt_secret
FRONTEND_URL=http://localhost:5173

# SMTP settings (optional) - when configured the server will send reset emails
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
FROM_EMAIL=no-reply@sommys.store
